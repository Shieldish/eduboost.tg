services:

  frontend:
    build: .
    container_name: gte-frontend
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ""
    networks:
      - gte_network

networks:
  gte_network:
    external: true
